"use client";
import React from "react";

import { ClipLoader } from "react-spinners";

const VerifyAndResetPassword = ({ token }: { token: string }) => {
  return <></>;
};

export default VerifyAndResetPassword;
