"use client";

import DashboardContent from "@/Components/Dashboard/DashboardContent";

const Dashboard = () => {
  // Handle plan selection after login (for Google OAuth flow)

  return <DashboardContent />;
};

export default Dashboard;
